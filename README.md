# CashTrack Web

Clean project layout:

- `frontend/` - Next.js app for Vercel.
- `backend/` - Supabase backend notes and API responsibilities.
- `database/` - Supabase SQL schema and security policies.

Start with `database/schema.sql`, then configure `frontend/.env.local`, then deploy `frontend/` to Vercel.
