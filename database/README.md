# Database

Run `schema.sql` in the Supabase SQL Editor.

It creates:

- `expenses`
- `paychecks`
- Row Level Security policies for user-owned data

After running the schema, copy your Supabase URL and anon key into `frontend/.env.local`.
